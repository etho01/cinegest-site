apiVersion: v1
kind: Service
metadata:
  name: cinegest-site
  namespace: cinegest-site
spec:
  selector:
    app: cinegest-site
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP